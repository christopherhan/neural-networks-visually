# Stage 5: Part IV Chapters 15–18 Implementation Plan

> **Execution mode:** implementer subagent per task, coordinator verifies inline (per user direction). Contract-altitude plan per the Stage 4 precedent: exact behavior contracts + prose beats; figure internals follow the established FigureShell/house-rules pattern.

**Goal:** Part IV — Mixture-of-Experts (15), Attention at Scale (16), Anatomy of a Frontier Model (17), Post-Training & Reasoning (18) — completing all 18 chapters.

**Honesty rules for this stage (CRITICAL — frontier-architecture claims must be labeled):** Exact internals of proprietary frontier models (Kimi K3, Fable-class systems) are not public. Every specific number presented must be either (a) from published open work (DeepSeek-V3/R1, Kimi K2 papers, Llama/Mixtral reports) and attributed as such, or (b) explicitly labeled "representative of the published frontier-family recipe". No figure or prose may claim to show K3's/Fable's actual internals. The ch17 figure carries a standing disclosure line.

**Figure inventory (Svelte 5 islands, `client:visible`, FigureShell, house rules; the MoE figure fetches model JSON — same loading/retry pattern via `url()`):**

| ch | component | behavior contract |
|---|---|---|
| 15 | `moe/MoERouter.svelte` | Fetches tokenizer.json + weights.json (browser-cached from ch10/13). Default sentence "The Queen said Alice must run faster" tokenized to chips. 8 experts as columns. A seeded (mulberry32(5)) random gate matrix (64→8) projects each token's REAL embedding to gate scores; softmax → top-2 routing. Honest note: the gate is untrained — the point is the mechanics, and that similar tokens (similar embeddings) route similarly. Click/focus a token chip → its top-2 experts highlight with gate % bars; lines/arrows from chip to experts. Aggregate load bars under experts (count of routed tokens, imbalance visible). FLOPs readout toggle: dense (all 8 experts/token) vs sparse top-2 → "4× less compute per token, same parameter pool" with the actual multiply counts for this toy. |
| 16 | `scale/KVCalculator.svelte` | No model. Two parts: (a) "why cache" mini-stepper (3 steps, reuse stepper pattern): generating token T without cache recomputes all keys/values every step (O(T²) total) → with cache, each step computes ONE new K,V and reuses the rest (O(T)) → the price: the cache lives in GPU memory and grows with context. (b) Calculator: sliders context length (2^10..2^20, log steps, default 128k), layers (24..96, default 61), d_head fixed 128 note, KV heads via preset buttons: MHA (128 heads), GQA (8 KV heads, "Llama-70B-family"), MLA ("DeepSeek/K2-family latent, ≈1/12 of GQA's already-reduced footprint — published family figures"); readout: KV cache bytes/token and total at chosen context (fp16), rendered as comparison bars for the three presets at the current context. Formula visible: 2 · layers · kv_heads · d_head · 2 bytes. |
| 17 | `anatomy/FrontierAnatomy.svelte` | No model. Vertical architecture explorer of a REPRESENTATIVE frontier MoE stack (standing disclosure under the title: "Representative of the published DeepSeek-V3 / Kimi-K2 family recipe — frontier labs' exact production internals (K3, Fable-class) are not public."). Regions (real buttons, panel pattern like TrainingPipeline): Embedding (byte-level BPE ~160k vocab) → 61× Block [MLA attention (KV compressed to a 512-d latent) + MoE FFN (1 shared expert + 256 routed, top-8 active)] → LM head. Fact panel per region with published-family numbers: total ≈1T params, ≈32B active per token, 128k context, MTP (multi-token prediction) training objective one-liner. A params bar: total vs active (visualizing the 30× sparsity win). |
| 18 | `rlhf/AlignmentLoop.svelte` | No model. 5-step stepper (established pattern): 1 policy generates candidate answers → 2 preferences collected (human raters and, increasingly, AI raters against a constitution) → 3 reward model trained to predict preferences → 4 policy optimized against reward model (RL) → 5 loop repeats; plus a second mini-tab/toggle "reasoning training": verifiable-reward RL (math/code where correctness is checkable) teaches models to SPEND tokens thinking — a scratchpad the reward shapes into genuine working-out. Exact explain strings authored by implementer in the site voice; keep each ≤ 2 sentences. |

**Chapters (frontmatter contract):** 15 `mixture-of-experts` "Mixture-of-Experts" · 16 `attention-at-scale` "Attention at Scale" · 17 `anatomy-of-a-frontier-model` "Anatomy of a Frontier Model" · 18 `post-training-and-reasoning` "Post-Training & Reasoning".

**Prose beats:**

- **ch15**: ch11's parting fact — 2/3 of params live in MLPs — becomes the lever: what if each token only used the MLP capacity it needs? MoE: replace the block's one MLP with many experts + a tiny router; each token activates top-k → parameters (knowledge) scale without compute scaling [figure] → the engineering tax: load balancing (aux losses / bias tricks — one-liner), all-to-all communication, memory to hold all experts → published-family framing: Mixtral 8×7B (open, top-2), DeepSeek-V3/K2 (256 routed + 1 shared, top-8, ≈1T total/≈32B active — cited as published figures). MathAside: MoE FFN y = Σ_{i∈topk} g_i E_i(x); compute ratio active/total. EngineerFooter: MoE = sharding you can't avoid; router stability is the ops pain; "1T parameters" headlines vs ~32B active — read spec sheets accordingly.
- **ch16**: generation revisits ch13's loop at scale — recompute vs cache [stepper]; the cache becomes THE bottleneck at long context (do the math live in the calculator: MHA at 128k explodes; GQA shares K/V across query-head groups; MLA compresses K/V into a small latent and reconstructs — published DeepSeek innovation adopted by the K2 family) → quadratic attention compute vs linear-ish memory: separate axes, both attacked → long-context reality: position stretching (RoPE scaling one-liner, callback ch12), sparse/windowed variants exist but full attention keeps winning at the frontier so far. MathAside: KV bytes = 2·L·H_kv·d_head·precision·T; GQA divides H_kv; MLA replaces H_kv·d_head with latent dim. EngineerFooter: inference is a memory-bandwidth business; batch size × context × KV bytes = your GPU bill; this is why "1M context" is a systems achievement, not a modeling one.
- **ch17**: the assembly chapter — walk the representative stack top to bottom [figure], explicitly connecting every part back to its chapter (embeddings→9, MLA→16, MoE→15, blocks→11, next-token head→13, the training pipeline→14) — the reader can now read a frontier architecture diagram the way an engineer reads a system diagram; honesty paragraph: what labs publish vs keep private (exact data, RL details, eval internals; K3/Fable internals unpublished — this diagram is the published family recipe) → the punchline: nothing in this tower is magic; every box is a chapter you've already read. MathAside: parameter accounting of the representative stack (embedding + 61·(attn + shared + 256·expert) ≈ totals; active-path accounting → ≈32B). EngineerFooter: a frontier model is a distributed system whose config file happens to be called "architecture"; the moat is data+RL+infra as much as shape.
- **ch18** (finale): base models predict, assistants behave — the gap is post-training [AlignmentLoop] → RLHF mechanics honestly (reward hacking risk one-liner: optimize a proxy hard enough and it breaks — why iteration + care matter) → reasoning models: RL on verifiable rewards teaches spending inference compute on a thinking scratchpad; "thinking" = sampled tokens the training made genuinely useful — more compute at answer time, better answers; the test-time-compute scaling axis → close the whole site: from an `if` statement (ch1) to a reasoning frontier model — the same weighted sums, all the way up; what we didn't cover (multimodality, agents, safety research — a paragraph of honest pointers) → final line returns to the trail map: journey complete. EngineerFooter: alignment is a product+safety discipline, not a final layer; evals are the release gates; if you remember one thing — the loop is simple, the consequences aren't.

**Tasks:** 1: ch15 · 2: ch16 · 3: ch17 · 4: ch18 · 5: integration (suite, 18 trail links, full chain 14→15→16→17→18, base-path guard, merge, deploy, live verify all four + spot-check the completed trail map).
